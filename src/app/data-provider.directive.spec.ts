import { DataProviderDirective } from './data-provider.directive';

describe('DataProviderDirective', () => {
  it('should create an instance', () => {
    const directive = new DataProviderDirective();
    expect(directive).toBeTruthy();
  });
});
